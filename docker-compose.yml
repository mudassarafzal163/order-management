version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./app:/app/app
      - sqlite_data:/app/instance
    environment:
      - FLASK_ENV=development
    restart: unless-stopped

volumes:
  sqlite_data: